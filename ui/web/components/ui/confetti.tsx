/**
 * Confetti — DEPRECATED.
 *
 * The "paper confetti on success" pattern is the AI-default celebratory
 * decoration (Leonxlnx/taste-skill §5.B and §9.A imply it). For a clinical
 * product where a successful draft is a routine event (not a launch), a
 * short, intentional checkmark draw + soft fade is more appropriate.
 *
 * Kept as a no-op so existing imports don't break the build.
 */
export function Confetti(_props: Record<string, unknown>): null {
    return null;
}

export default Confetti;
