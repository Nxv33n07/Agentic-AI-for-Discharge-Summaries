/**
 * Marquee — DEPRECATED.
 *
 * Horizontal scrolling text marquee. Per taste-skill §5 "MARQUEE
 * MAX-ONE-PER-PAGE" and §9.A: appropriate at most once per page. We don't
 * have a place where a marquee earns its keep in a clinical product, so
 * this is retired.
 *
 * Kept as a no-op so existing imports don't break the build.
 */
export function Marquee(_props: Record<string, unknown>): null {
    return null;
}

export default Marquee;
