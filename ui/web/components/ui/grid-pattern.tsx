/**
 * GridPattern — DEPRECATED.
 *
 * Decorative SVG grid pattern, removed per the taste-skill anti-slop audit
 * (Leonxlnx/taste-skill §9.F: "NO crosshair / hairline grid lines as
 * decoration ... Use them only when they organize real content.").
 *
 * Kept as a no-op so existing imports don't break the build.
 */
export function GridPattern(_props: Record<string, unknown>): null {
    return null;
}

export default GridPattern;
