# Dscribe Web UI environment template
# Copy to .env.local for local development.

# Public URL of the Dscribe FastAPI backend.
# Used by the Next.js client (browser-visible).
NEXT_PUBLIC_API_URL=http://localhost:8000
